services:
  frontend:
    build:
      context: ./frontend
      args:
        # NEXT_PUBLIC_* vars are baked into JS at build time.
        # Backend now lives on Brain 2 (10.0.0.11:8300)
        NEXT_PUBLIC_API_URL: http://10.0.0.11:8300
        NEXT_PUBLIC_WS_URL: ws://10.0.0.1:18789
    ports:
      - "3300:3000"
    restart: unless-stopped

  # Backend moved to Brain 2 — see ~/warroom/docker-compose.yml on 10.0.0.11
  # Mental Library backend moved to Brain 2 as well
